{
  "name": "typeforge-workspace",
  "version": "0.0.0",
  "private": true,
  "description": "TypeForge — AI-Powered TypeScript Mastery Platform (monorepo root)",
  "workspaces": [
    "apps/*",
    "packages/*",
    "libs/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev": "concurrently \"npm run dev -w apps/api\" \"npm run dev -w apps/web\"",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
