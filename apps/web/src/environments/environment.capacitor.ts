export const environment = {
  production: false,
  // Point at your local dev API while testing in a simulator/emulator.
  // For a device on the same network, replace localhost with your machine's LAN IP.
  apiBase: 'http://localhost:3000/api',
  wsBase: 'http://localhost:3000',
};
